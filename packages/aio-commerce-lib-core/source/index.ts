/**
 * @privateRemarks This annotations are used by Typedoc to correctly generate the API reference. Don't remove them.
 *
 * @module index
 * @mergeModuleWith <project>
 */

/**
 * @privateRemarks
 * Dummy file for Typedoc to correctly generate API reference file structure.
 * **Remove this file** when the package has more than one sub-module.
 *
 * (At the time of writing, there's only the `error` sub-module.)
 */


---
"@adobe/aio-commerce-lib-config": minor
---

Rename the `base_url` input parameter of `sync-commerce-scopes` and remove the possibility to get if from `.env`. Add new input for specifying the commerce flavor.

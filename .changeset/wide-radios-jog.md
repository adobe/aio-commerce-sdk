---
"@adobe/aio-commerce-plugin-app-migration": patch
---

The `commerce-app-migrate` skill now assembles `upgradeMode: "manual"` by default, matching the SDK's temporary default while automatic upgrade execution is stabilized.

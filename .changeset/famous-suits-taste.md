---
"@adobe/aio-commerce-sdk": major
---

Picks up the removal of the deprecated `PUT /config` action endpoint from `@adobe/aio-commerce-lib-app`. Use `PATCH /config` instead, which supports partial updates and unset semantics.

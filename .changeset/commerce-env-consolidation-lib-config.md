---
"@adobe/aio-commerce-lib-config": patch
---

The optional field `env` scoping now uses the shared Commerce environment schema from `@adobe/aio-commerce-lib-core/commerce`. Accepted values (`"paas"`, `"saas"`) and validation messages are unchanged.

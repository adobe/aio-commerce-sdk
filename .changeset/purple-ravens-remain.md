---
"@adobe/aio-commerce-lib-app": minor
---

Add upgrade planning support for webhook additions and removals: the webhooks step now diffs the baseline snapshot against the target configuration to add newly configured webhooks and remove ones dropped from it.

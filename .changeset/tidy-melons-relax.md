---
"@adobe/aio-commerce-plugin-app-management": patch
---

Update the `commerce-app-init`, `commerce-app-admin-ui`, `commerce-app-eventing`, `commerce-app-webhooks`, and `commerce-app-storage` skills to document that actions and custom installation step scripts can be authored directly in TypeScript once the project is configured for it, without a separate compile step.

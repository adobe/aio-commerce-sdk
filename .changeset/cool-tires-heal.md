---
"@adobe/aio-commerce-plugin-app-management": patch
---

The `commerce-app-init` skill now sets `upgradeMode: "manual"` in generated configs, matching the SDK's temporary default while automatic upgrade execution is stabilized.

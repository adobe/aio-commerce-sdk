---
"@adobe/aio-commerce-lib-admin-ui": minor
---

Add `invoice`, `creditmemo`, and `shipment` grid types to the grid column wire contract, so runtime action handlers and `getGridColumnAclResourceId` can target those grids in addition to order, product, and customer.

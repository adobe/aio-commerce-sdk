---
"@adobe/aio-commerce-lib-config": minor
---

Enhance the library with the new resolver helpers of `@adobe/aio-commerce-lib-api` and the response helpers of `@adobe/aio-commerce-lib-core` to remove unnecessary logic. The library no longer exports `buildCommerceHttpClient`, `CommerceValidationError`, `createErrorResponse` and `createSuccessResponse`

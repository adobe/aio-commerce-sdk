---
"@adobe/aio-commerce-lib-config": minor
---

Enhance the library with the new resolver helpers of `@adobe/aio-commerce-lib-api` to remove unnecessary logic and the response helpers of `@adobe/aio-commerce-lib-core`. The library no longer exports `buildCommerceHttpClient`, `CommerceValidationError`, `createErrorResponse` and `createSuccessResponse`

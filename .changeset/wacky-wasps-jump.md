---
"@adobe/aio-commerce-lib-auth": minor
---

Introduced CLI utility `sync-ims-credentials` to sync the workspace credentials of `.aio` into the `.env` file environment variables expected by the library

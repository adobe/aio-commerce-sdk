---
"@adobe/aio-commerce-lib-api": minor
---

Add `unwrapHttpError` utility under `@adobe/aio-commerce-lib-api/utils` for extracting readable messages from ky `HTTPError` response bodies, with support for Commerce-style `%1`/`%2` parameter interpolation.

---
"@adobe/aio-commerce-lib-app": minor
"@adobe/aio-commerce-plugin-app-management": minor
---

Run your own code in the generated `pre-app-build`, `pre-app-run`, `pre-app-dev` and `post-app-deploy` hooks by adding a `hooks/<hook>` file at the project root, or under an extension point folder to override it for that extension point.
